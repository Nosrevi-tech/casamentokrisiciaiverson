# Chave de criptografia (obrigatório em produção)
ENCRYPTION_KEY=sua_chave_de_32_caracteres_muito_segura

# Porta do servidor (opcional - padrão: 3001)
PORT=3001

# Ambiente (opcional - padrão: development)
NODE_ENV=production